# frozen_string_literal: true

# Local stub satisfying openhab-scripting's "openhab" runtime version dependency (see Gemfile
# comment). Never required; carries no code.
Gem::Specification.new do |spec|
  spec.name    = "openhab"
  spec.version = "5.2.0"
  spec.summary = "Stub gem satisfying openhab-scripting's runtime dependency for local tooling"
  spec.authors = ["N/A"]
  spec.files   = []
end
